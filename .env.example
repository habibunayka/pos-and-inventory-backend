# Copy this file to `.env` and adjust the values for your environment before running the server.

# Connection string consumed by Prisma's Postgres datasource.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://postgres:dev@localhost:5432/pos_db?schema=public"
JWT_SECRET="ISI_DISINI"
JWT_EXPIRES_IN="1d"

# Optional overrides for seeding default accounts.
# Uncomment and adjust to customize seeded credentials when running `npm run prisma:seed`.
# SEED_OWNER_NAME="F&B Owner"
# SEED_OWNER_EMAIL="owner@example.com"
# SEED_OWNER_PASSWORD="OwnerPass123!"
# SEED_MANAGER_NAME="Outlet Manager"
# SEED_MANAGER_EMAIL="manager@example.com"
# SEED_MANAGER_PASSWORD="ManagerPass123!"
# SEED_SUPERVISOR_NAME="Shift Supervisor"
# SEED_SUPERVISOR_EMAIL="supervisor@example.com"
# SEED_SUPERVISOR_PASSWORD="SupervisorPass123!"
# SEED_CASHIER_NAME="Cashier A"
# SEED_CASHIER_PIN="123456"

# Provide additional seeded accounts as a JSON array if needed.
# Example:
# SEED_ADDITIONAL_ACCOUNTS='[{"name":"Inventory Admin","role":"manager","email":"inventory@example.com","password":"Inv3ntory!"}]'

# After setting the DATABASE_URL, run the Prisma migrations to sync the schema:
#   npm install
#   npm run prisma:migrate
# Optionally seed the database if required:
#   npm run prisma:seed
# With the database prepared, you can start the HTTP server:
#   npm start
